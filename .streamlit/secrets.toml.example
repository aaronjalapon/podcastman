# =============================================================================
# podcastman — Streamlit Cloud Secrets
# =============================================================================
# Copy this file to .streamlit/secrets.toml and fill in your values.
# On Streamlit Community Cloud, paste these into the "Secrets" section
# in the app settings dashboard.
#
# These values are bridged to environment variables at startup so the
# pydantic-settings backend picks them up automatically.
# =============================================================================

# LLM (required — choose one provider)
LLM_MODEL = "gpt-4o"
LLM_API_KEY = ""

# Embeddings (required — usually same provider as LLM)
EMBEDDING_MODEL = "text-embedding-3-small"
EMBEDDING_API_KEY = ""

# Optional overrides (defaults are fine for most deployments)
# COQUI_MODEL_NAME = "tts_models/multilingual/multi-dataset/xtts_v2"
# TTS_LANGUAGE = "en"
# LOG_LEVEL = "INFO"
